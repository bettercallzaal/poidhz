# R7 Bountycaster cross-post (READY TO SEND alongside the POIDH cast)

Per ZAOOS doc 1584: Bountycaster (bountycaster.xyz, indexed via @bountybot + the
/bounties channel) reaches 200-400 Farcaster-native builders who browse for gigs
but won't see poidh.xyz or a GitHub issue on their own. POIDH handles escrow,
this handles discovery. Post same day as the POIDH cast, not before (need the
POIDH URL to link).

Uses R7's actual reward framing (per-fix, judge's call, not winner-take-all) -
not doc 1584's placeholder ZABAL/USDC numbers, which were never locked into the
real spec.

## Cast (post to /bounties primary, /zabal secondary)

```
🔵 Bounty — ZABAL Gamez Bug Fixes

zabalgamez.com has bugs. find a real one, fix it, get paid.

This is a code bounty - submissions are pull requests, not clips.

→ find a real bug (broken page, dead link, mobile break, a flow that dead-ends)
→ open a public PR with the fix (or a clear writeup + patch if you can't PR)
→ prove it - screen recording or before/after screenshots
→ submit the PR/writeup URL on the POIDH page below

Not winner-take-all - multiple strong fixes each get paid. Scope + payout per
fix at the judge's call.

Judged by @bettercallzaal. POIDH escrow on Base: [POIDH R7 URL after casting]

Reply to claim a bug. Code -> merge -> earn.

@bountybot
```

## Before sending

- [ ] R7 must be cast on POIDH first (via `docs/create-bounty.html`) to get the escrow URL
- [ ] Fill in the POIDH URL above
- [ ] Post to /bounties (primary, this is what @bountybot indexes) and /zabal (community visibility)
- [ ] Optional: cross-post to X with #web3bounty
